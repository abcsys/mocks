apiVersion: mock.digi.dev/v1
kind: Room
metadata:
  name: {{ .Values.name }}
spec:
  control:
    brightness:
      intent: 0.4
    mode:
      intent: idle
#  reflex:
#    motion-mode:
#      policy: 'if any(.obs.objects; . == "")  <= 600 then .root.control.mode.intent = "work" else . end'
#        priority: 1
#        processor: jq